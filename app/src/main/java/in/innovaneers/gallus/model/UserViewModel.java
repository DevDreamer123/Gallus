package in.innovaneers.gallus.model;

import androidx.lifecycle.LiveData;
import androidx.lifecycle.ViewModel;


import in.innovaneers.gallus.model.Repository;

/*public class UserViewModel extends ViewModel {
    private final Repository repository;
@Inject
    public UserViewModel(Repository repository){
        this.repository = repository;
    }
    public LiveData<Result<LoginResponseModel>> login (LoginRequestModel loginRequestModeli){
        return repository.login(loginRequestModeli);
    }
}*/
